async function test(): Promise<void> {
    console.log("OpenFlex Store");
    console.log("Initializing");

    await loadProducts();

    const token = getAuthToken();
    if (token) {
        console.log("Session restored");
    }
}

test();
