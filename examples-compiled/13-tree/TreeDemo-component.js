// Generated by OpenFlex Neo MXML Compiler
// Complete MXML -> Web Components with Full Reactivity

(function() {

  // Generated by OpenFlex Neo
  // AS4 -> JavaScript

  // OpenFlex Reactivity Runtime
  const { Signal, Computed, createEffect } = (typeof window !== 'undefined' && window.OpenFlexRuntime)
    ? window.OpenFlexRuntime
    : require('./runtime/openflex-runtime.js');

  const fileSystem = new Signal([]);

  const categories = new Signal([]);

  const selectedFile = new Signal('');

  const selectedCategory = new Signal('');

  const lastAction = new Signal('Click on a tree node to see actions');

  async function loadExternalData() {
    try {
      const response = await fetch('./tree-demo-data.json');
      const data = await response.json();
      fileSystem.value = data.fileSystem;
      categories.value = data.categories;
    } catch (error) {
      console.error('Error loading data:', error);
    }
  }

  function onFileSelect(event) {
    selectedFile.value = event.item.name;
    lastAction.value = 'Selected: ' + event.item.name;
  }

  function onNodeToggle(item, isOpen) {
    lastAction.value = (isOpen ? 'Opened: ' : 'Closed: ') + item.name;
  }

  function onCategorySelect(event) {
    selectedCategory.value = event.item.label;
    lastAction.value = 'Category: ' + event.item.label;
  }


  // Application Component
  class AppComponent extends HTMLElement {
    constructor() {
      super();
      this.attachShadow({ mode: 'open' });
    }

    connectedCallback() {
      this.render();
      loadExternalData().then(() => {
        this.setupReactivity();
      });
    }

    render() {
      this.shadowRoot.innerHTML = `
        <link rel='stylesheet' href='../runtime/neo-flex-classic-theme.css'>
        <link rel='stylesheet' href='../runtime/openflex-ios-theme.css'>
        <link rel='stylesheet' href='./tree-demo.css'>
      <div id='app_0' class='neo-application'>
        <div id='vbox_1' class='neo-vbox app'>
          <div id='vbox_2' class='neo-vbox wrapper'>
            <div id='vbox_3' class='neo-vbox header'>
              <span id='label_4' class='neo-label title'>Tree Component</span>
              <span id='label_5' class='neo-label subtitle'>Hierarchical data with expand/collapse</span>
            </div>
            <div id='hbox_6' class='neo-hbox info-banner'>
              <span id='label_7' class='neo-label info-icon'>🌳</span>
              <span id='label_8' class='neo-label info-text'>The Tree component displays hierarchical data with nested folders and expand/collapse functionality.</span>
            </div>
            <div id='hbox_9' class='neo-hbox grid'>
              <div id='vbox_10' class='neo-vbox card'>
                <div id='vbox_11' class='neo-vbox card-header'>
                  <span id='label_12' class='neo-label card-title'>File System</span>
                  <span id='label_13' class='neo-label card-subtitle'>With icons and file sizes</span>
                </div>
                <div id='vbox_14' class='neo-vbox tree-container'>
                  <div id='tree_15' class='neo-tree'>
                    <!-- Tree nodes will be dynamically generated -->
                  </div>
                </div>
                <div id='hbox_16' class='neo-hbox selection-info'>
                  <span id='label_17' class='neo-label selection-label'>Selected:</span>
                  <span id='label_18' class='neo-label selection-value'></span>
                </div>
              </div>
              <div id='vbox_19' class='neo-vbox card'>
                <div id='vbox_20' class='neo-vbox card-header'>
                  <span id='label_21' class='neo-label card-title'>Categories</span>
                  <span id='label_22' class='neo-label card-subtitle'>Auto-expanded navigation</span>
                </div>
                <div id='vbox_23' class='neo-vbox tree-container'>
                  <div id='tree_24' class='neo-tree'>
                    <!-- Tree nodes will be dynamically generated -->
                  </div>
                </div>
                <div id='hbox_25' class='neo-hbox selection-info'>
                  <span id='label_26' class='neo-label selection-label'>Selected:</span>
                  <span id='label_27' class='neo-label selection-value'></span>
                </div>
              </div>
            </div>
            <div id='hbox_28' class='neo-hbox status-bar'>
              <span id='label_29' class='neo-label status-icon'>📋</span>
              <span id='label_30' class='neo-label status-text'></span>
            </div>
          </div>
        </div>
      </div>
      `;

      // Attach event handlers
      this.attachEventHandlers();
    }

    attachEventHandlers() {
    }

    setupReactivity() {
      // Tree: tree_15
      createEffect(() => {
        const el = this.shadowRoot.getElementById('tree_15');
        if (!el) return;

        // Tree state tracking
        if (!el._openNodes) el._openNodes = new Set();
        const defaultOpen = false;

        // Recursive function to render tree nodes
        function renderNode(node, depth, path, container) {
          const nodeEl = document.createElement('div');
          nodeEl.className = 'neo-tree-node';
          nodeEl.dataset.path = path;

          const itemEl = document.createElement('div');
          itemEl.className = 'neo-tree-item';
          itemEl.style.paddingLeft = (depth * 8) + 'px';

          // Disclosure triangle
          const disclosureEl = document.createElement('span');
          disclosureEl.className = 'neo-tree-disclosure';
          const hasChildren = node.children && node.children.length > 0;
          if (!hasChildren) disclosureEl.classList.add('empty');

          // Check if node is open
          const isOpen = el._openNodes.has(path) || (defaultOpen && hasChildren && !el._initialized);
          if (isOpen && hasChildren) {
            disclosureEl.classList.add('open');
            el._openNodes.add(path);
          }

          // Icon
          const iconEl = document.createElement('span');
          if (node.icon) {
            iconEl.className = 'neo-tree-icon custom';
            iconEl.textContent = node.icon;
          } else {
            iconEl.className = 'neo-tree-icon ' + (hasChildren ? (isOpen ? 'folder open' : 'folder') : 'file');
          }

          // Label
          const labelEl = document.createElement('span');
          labelEl.className = 'neo-tree-label';
          labelEl.textContent = node.name || '';

          // Assemble item
          itemEl.appendChild(disclosureEl);
          itemEl.appendChild(iconEl);
          itemEl.appendChild(labelEl);
          nodeEl.appendChild(itemEl);

          // Children container
          const childrenEl = document.createElement('div');
          childrenEl.className = 'neo-tree-children' + (isOpen ? ' open' : '');

          // Click handlers
          const hasCustomIcon = !!node.icon;
          disclosureEl.addEventListener('click', (e) => {
            e.stopPropagation();
            if (!hasChildren) return;
            const wasOpen = el._openNodes.has(path);
            if (wasOpen) {
              el._openNodes.delete(path);
              disclosureEl.classList.remove('open');
              if (!hasCustomIcon) iconEl.className = 'neo-tree-icon folder';
              childrenEl.classList.remove('open');
            } else {
              el._openNodes.add(path);
              disclosureEl.classList.add('open');
              if (!hasCustomIcon) iconEl.className = 'neo-tree-icon folder open';
              childrenEl.classList.add('open');
            }
          });

          itemEl.addEventListener('click', (e) => {
          });

          // Render children recursively
          if (hasChildren) {
            node.children.forEach((child, idx) => {
              renderNode(child, depth + 1, path + '.' + idx, childrenEl);
            });
          }
          nodeEl.appendChild(childrenEl);
          container.appendChild(nodeEl);
        }

        // Clear previous content
        el.innerHTML = '';

        // Render tree
        const treeData = fileSystem.value || [];
        treeData.forEach((node, idx) => {
          renderNode(node, 0, String(idx), el);
        });

        el._initialized = true;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_18');
        if (el) el.textContent = selectedFile.value || 'None';
      });
      // Tree: tree_24
      createEffect(() => {
        const el = this.shadowRoot.getElementById('tree_24');
        if (!el) return;

        // Tree state tracking
        if (!el._openNodes) el._openNodes = new Set();
        const defaultOpen = true;

        // Recursive function to render tree nodes
        function renderNode(node, depth, path, container) {
          const nodeEl = document.createElement('div');
          nodeEl.className = 'neo-tree-node';
          nodeEl.dataset.path = path;

          const itemEl = document.createElement('div');
          itemEl.className = 'neo-tree-item';
          itemEl.style.paddingLeft = (depth * 8) + 'px';

          // Disclosure triangle
          const disclosureEl = document.createElement('span');
          disclosureEl.className = 'neo-tree-disclosure';
          const hasChildren = node.children && node.children.length > 0;
          if (!hasChildren) disclosureEl.classList.add('empty');

          // Check if node is open
          const isOpen = el._openNodes.has(path) || (defaultOpen && hasChildren && !el._initialized);
          if (isOpen && hasChildren) {
            disclosureEl.classList.add('open');
            el._openNodes.add(path);
          }

          // Icon
          const iconEl = document.createElement('span');
          if (node.icon) {
            iconEl.className = 'neo-tree-icon custom';
            iconEl.textContent = node.icon;
          } else {
            iconEl.className = 'neo-tree-icon ' + (hasChildren ? (isOpen ? 'folder open' : 'folder') : 'file');
          }

          // Label
          const labelEl = document.createElement('span');
          labelEl.className = 'neo-tree-label';
          labelEl.textContent = node.label || '';

          // Assemble item
          itemEl.appendChild(disclosureEl);
          itemEl.appendChild(iconEl);
          itemEl.appendChild(labelEl);
          nodeEl.appendChild(itemEl);

          // Children container
          const childrenEl = document.createElement('div');
          childrenEl.className = 'neo-tree-children' + (isOpen ? ' open' : '');

          // Click handlers
          const hasCustomIcon = !!node.icon;
          disclosureEl.addEventListener('click', (e) => {
            e.stopPropagation();
            if (!hasChildren) return;
            const wasOpen = el._openNodes.has(path);
            if (wasOpen) {
              el._openNodes.delete(path);
              disclosureEl.classList.remove('open');
              if (!hasCustomIcon) iconEl.className = 'neo-tree-icon folder';
              childrenEl.classList.remove('open');
            } else {
              el._openNodes.add(path);
              disclosureEl.classList.add('open');
              if (!hasCustomIcon) iconEl.className = 'neo-tree-icon folder open';
              childrenEl.classList.add('open');
            }
          });

          itemEl.addEventListener('click', (e) => {
          });

          // Render children recursively
          if (hasChildren) {
            node.children.forEach((child, idx) => {
              renderNode(child, depth + 1, path + '.' + idx, childrenEl);
            });
          }
          nodeEl.appendChild(childrenEl);
          container.appendChild(nodeEl);
        }

        // Clear previous content
        el.innerHTML = '';

        // Render tree
        const treeData = categories.value || [];
        treeData.forEach((node, idx) => {
          renderNode(node, 0, String(idx), el);
        });

        el._initialized = true;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_27');
        if (el) el.textContent = selectedCategory.value || 'None';
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_30');
        if (el) el.textContent = lastAction.value;
      });
    }
  }

  // Register Custom Element
  if (typeof window !== 'undefined') {
    customElements.define('app-root', AppComponent);
  }

  // Export for Node.js/testing
  if (typeof module !== 'undefined' && module.exports) {
    module.exports = { AppComponent };
  }

})();
