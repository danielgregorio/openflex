// Generated by OpenFlex Neo MXML Compiler
// MXML -> Web Components with Reactivity

const { Signal, Computed, createEffect } = require('./runtime/openflex-runtime.js');
const todos = new Signal([]);
const filter = new Signal("all");
const newTodoText = new Signal("");
let nextId = 1;
const filteredTodos = new Computed(() => t => !t.completed);
if (filter.value = "completed") {
  return todos.value.filter.value(t => t.completed);
}
return todos.value;
return todos.value.length;
return todos.value.filter.value(t => !t.completed).length;
return todos.value.filter.value(t => t.completed).length;
return completedCount > 0;
function dTodo()() {
  if (wTodoText.t.im()() + ) )   return;
  dos = + [{ : : -xtId++, xt: : wTodoText.t.im()(), mpleted: : false }];
  wTodoText = + ; ;
}
function ggleTodo(i(: ) {
  dos = + dos.m.p(t(do = => do.i. = +  ? ? { mpleted: : -do.c.mpleted}  } : do
 );
}
function leteTodo(i(: ) {
  dos = + dos.f.lter(t(= => i. ! + ););
}
function earCompleted()() {
  dos = + dos.f.lter(t(= => -c.mpleted););
}
function tFilter(n(wFilter: ) {
  lter = + wFilter;
;
}
function ndleKeyPress(e(ent):) {
  if (ent.k.y = + nter") ) {
    dTodo()();
  }
}

// Application Component
class AppComponent extends HTMLElement {
  constructor() {
    super();
    this.attachShadow({ mode: 'open' });
  }

  connectedCallback() {
    this.render();

    // Set up reactive effects
    this.setupReactivity();
  }

  render() {
    this.shadowRoot.innerHTML = `
      <div class='application'>
        <div class='vbox'>
        </div>
      </div>
    `;

    // Attach event handlers
  }

  setupReactivity() {
    // Auto-update DOM when reactive state changes
  }
}

// Register Custom Elements
if (typeof window !== 'undefined') {
  customElements.define('app-root', AppComponent);
}

// Export for Node.js/testing
if (typeof module !== 'undefined' && module.exports) {
  module.exports = { AppComponent };
}