// Generated by OpenFlex Neo MXML Compiler
// Complete MXML -> Web Components with Full Reactivity

(function() {

  // Generated by OpenFlex Neo
  // AS4 -> JavaScript

  // OpenFlex Reactivity Runtime
  const { Signal, Computed, createEffect } = (typeof window !== 'undefined' && window.OpenFlexRuntime)
    ? window.OpenFlexRuntime
    : require('./runtime/openflex-runtime.js');

  const email = new Signal('');

  const password = new Signal('');

  const age = new Signal('');

  const phone = new Signal('');

  const emailError = new Signal('');

  const passwordError = new Signal('');

  const ageError = new Signal('');

  const phoneError = new Signal('');

  const formResult = new Signal('');

  const formSuccess = new Signal(false);

  function validateEmail() {
    let pattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (email.value.length === 0) {
      emailError.value = '';
    }
    else   if (pattern.test(email.value)) {
      emailError.value = 'valid';
    }
    else {
      emailError.value = 'Please enter a valid email';
    }
  }

  function validatePassword() {
    if (password.value.length === 0) {
      passwordError.value = '';
    }
    else   if (password.value.length >= 8) {
      passwordError.value = 'valid';
    }
    else {
      passwordError.value = 'Password must be at least 8 characters';
    }
  }

  function validateAge() {
    if (age.value.length === 0) {
      ageError.value = '';
      return;
    }
    let num = parseInt(age.value);
    if (isNaN(num)) {
      ageError.value = 'Please enter a number';
    }
    else   if (num >= 18) {
      ageError.value = 'valid';
    }
    else {
      ageError.value = 'Must be at least 18';
    }
  }

  function validatePhone() {
    if (phone.value.length === 0) {
      phoneError.value = '';
      return;
    }
    let digits = phone.value.replace(/\D/g, '');
    if (digits.length >= 10) {
      phoneError.value = 'valid';
    }
    else {
      phoneError.value = 'Enter at least 10 digits';
    }
  }

  function submitForm() {
    validateEmail();
    validatePassword();
    validateAge();
    validatePhone();
    let allValid = emailError.value === 'valid' && passwordError.value === 'valid' && ageError.value === 'valid' && phoneError.value === 'valid';
    if (allValid) {
      formResult.value = 'Form submitted successfully!';
      formSuccess.value = true;
    }
    else {
      formResult.value = 'Please fix the errors above.';
      formSuccess.value = false;
    }
  }

  function resetForm() {
    email.value = '';
    password.value = '';
    age.value = '';
    phone.value = '';
    emailError.value = '';
    passwordError.value = '';
    ageError.value = '';
    phoneError.value = '';
    formResult.value = '';
    formSuccess.value = false;
  }

  const emailIsValid = new Computed(() => {
    return emailError.value === 'valid';
  });

  const passwordIsValid = new Computed(() => {
    return passwordError.value === 'valid';
  });

  const ageIsValid = new Computed(() => {
    return ageError.value === 'valid';
  });

  const phoneIsValid = new Computed(() => {
    return phoneError.value === 'valid';
  });

  const emailMessage = new Computed(() => {
    return (emailError.value === 'valid' ? 'Valid email' : emailError.value);
  });

  const passwordMessage = new Computed(() => {
    return (passwordError.value === 'valid' ? 'Strong password' : passwordError.value);
  });

  const ageMessage = new Computed(() => {
    return (ageError.value === 'valid' ? 'Valid age' : ageError.value);
  });

  const phoneMessage = new Computed(() => {
    return (phoneError.value === 'valid' ? 'Valid phone' : phoneError.value);
  });


  // Application Component
  class AppComponent extends HTMLElement {
    constructor() {
      super();
      this.attachShadow({ mode: 'open' });
    }

    connectedCallback() {
      this.render();
      this.setupReactivity();
    }

    render() {
      this.shadowRoot.innerHTML = `
        <link rel='stylesheet' href='../runtime/neo-flex-classic-theme.css'>
        <link rel='stylesheet' href='./validators-demo.css'>
      <div id='app_0' class='neo-application'>
        <div id='vbox_1' class='neo-vbox app'>
          <div id='vbox_2' class='neo-vbox wrapper'>
            <div id='vbox_3' class='neo-vbox header'>
              <span id='label_4' class='neo-label title'>Form Validators</span>
              <span id='label_5' class='neo-label subtitle'>Real-time validation with visual feedback</span>
            </div>
            <div id='vbox_6' class='neo-vbox card'>
              <span id='label_7' class='neo-label card-title'>Registration Form</span>
              <div id='vbox_8' class='neo-vbox form-field'>
                <span id='label_9' class='neo-label field-label'>Email Address</span>
                <input type='text' id='input_10' class='neo-textinput' placeholder='you@example.com' />
                <div id='hbox_11' class='neo-hbox'>
                  <span id='label_12' class='neo-label'></span>
                  <span id='label_13' class='neo-label'></span>
                </div>
              </div>
              <div id='vbox_14' class='neo-vbox form-field'>
                <span id='label_15' class='neo-label field-label'>Password</span>
                <input type='text' id='input_16' class='neo-textinput' placeholder='Minimum 8 characters' />
                <div id='hbox_17' class='neo-hbox'>
                  <span id='label_18' class='neo-label'></span>
                  <span id='label_19' class='neo-label'></span>
                </div>
              </div>
              <div id='vbox_20' class='neo-vbox form-field'>
                <span id='label_21' class='neo-label field-label'>Age</span>
                <input type='text' id='input_22' class='neo-textinput' placeholder='18 or older' style='width: 120px' />
                <div id='hbox_23' class='neo-hbox'>
                  <span id='label_24' class='neo-label'></span>
                  <span id='label_25' class='neo-label'></span>
                </div>
              </div>
              <div id='vbox_26' class='neo-vbox form-field'>
                <span id='label_27' class='neo-label field-label'>Phone Number</span>
                <input type='text' id='input_28' class='neo-textinput' placeholder='10+ digits' />
                <div id='hbox_29' class='neo-hbox'>
                  <span id='label_30' class='neo-label'></span>
                  <span id='label_31' class='neo-label'></span>
                </div>
              </div>
              <div id='hbox_32' class='neo-hbox btn-row'>
                <button id='btn_33' class='neo-button btn btn-primary'>Submit</button>
                <button id='btn_34' class='neo-button btn btn-secondary'>Reset</button>
              </div>
              <span id='label_35' class='neo-label'></span>
            </div>
          </div>
        </div>
      </div>
      `;

      // Attach event handlers
      this.attachEventHandlers();
    }

    attachEventHandlers() {
      const el_btn_33 = this.shadowRoot.getElementById('btn_33');
      if (el_btn_33) el_btn_33.addEventListener('click', () => submitForm());
      const el_btn_34 = this.shadowRoot.getElementById('btn_34');
      if (el_btn_34) el_btn_34.addEventListener('click', () => resetForm());
    }

    setupReactivity() {
      createEffect(() => {
        const el = this.shadowRoot.getElementById('input_10');
        if (el) el.className = `field-input ${emailIsValid.value ? 'valid' : (emailError.value.length > 0 ? 'invalid' : '')}`;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('input_10');
        if (el) el.value = email.value;
      });
    
      // Two-way binding para input_10
      const el_input_10 = this.shadowRoot.getElementById('input_10');
      if (el_input_10) {
        el_input_10.addEventListener('input', (e) => {
          email.value = e.target.value;
        });
      }
      createEffect(() => {
        const el = this.shadowRoot.getElementById('hbox_11');
        if (el) el.className = `field-msg ${emailIsValid.value ? 'msg-valid' : 'msg-invalid'}`;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('hbox_11');
        if (el) el.style.display = (emailError.value.length > 0) ? 'block' : 'none';
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_12');
        if (el) el.textContent = emailIsValid.value ? '✓' : '✗';
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_13');
        if (el) el.textContent = emailMessage.value;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('input_16');
        if (el) el.className = `field-input ${passwordIsValid.value ? 'valid' : (passwordError.value.length > 0 ? 'invalid' : '')}`;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('input_16');
        if (el) el.value = password.value;
      });
    
      // Two-way binding para input_16
      const el_input_16 = this.shadowRoot.getElementById('input_16');
      if (el_input_16) {
        el_input_16.addEventListener('input', (e) => {
          password.value = e.target.value;
        });
      }
      createEffect(() => {
        const el = this.shadowRoot.getElementById('hbox_17');
        if (el) el.className = `field-msg ${passwordIsValid.value ? 'msg-valid' : 'msg-invalid'}`;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('hbox_17');
        if (el) el.style.display = (passwordError.value.length > 0) ? 'block' : 'none';
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_18');
        if (el) el.textContent = passwordIsValid.value ? '✓' : '✗';
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_19');
        if (el) el.textContent = passwordMessage.value;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('input_22');
        if (el) el.className = `field-input ${ageIsValid.value ? 'valid' : (ageError.value.length > 0 ? 'invalid' : '')}`;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('input_22');
        if (el) el.value = age.value;
      });
    
      // Two-way binding para input_22
      const el_input_22 = this.shadowRoot.getElementById('input_22');
      if (el_input_22) {
        el_input_22.addEventListener('input', (e) => {
          age.value = e.target.value;
        });
      }
      createEffect(() => {
        const el = this.shadowRoot.getElementById('hbox_23');
        if (el) el.className = `field-msg ${ageIsValid.value ? 'msg-valid' : 'msg-invalid'}`;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('hbox_23');
        if (el) el.style.display = (ageError.value.length > 0) ? 'block' : 'none';
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_24');
        if (el) el.textContent = ageIsValid.value ? '✓' : '✗';
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_25');
        if (el) el.textContent = ageMessage.value;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('input_28');
        if (el) el.className = `field-input ${phoneIsValid.value ? 'valid' : (phoneError.value.length > 0 ? 'invalid' : '')}`;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('input_28');
        if (el) el.value = phone.value;
      });
    
      // Two-way binding para input_28
      const el_input_28 = this.shadowRoot.getElementById('input_28');
      if (el_input_28) {
        el_input_28.addEventListener('input', (e) => {
          phone.value = e.target.value;
        });
      }
      createEffect(() => {
        const el = this.shadowRoot.getElementById('hbox_29');
        if (el) el.className = `field-msg ${phoneIsValid.value ? 'msg-valid' : 'msg-invalid'}`;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('hbox_29');
        if (el) el.style.display = (phoneError.value.length > 0) ? 'block' : 'none';
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_30');
        if (el) el.textContent = phoneIsValid.value ? '✓' : '✗';
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_31');
        if (el) el.textContent = phoneMessage.value;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_35');
        if (el) el.style.display = (formResult.value.length > 0) ? 'block' : 'none';
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_35');
        if (el) el.className = formSuccess.value ? 'result-success' : 'result-error';
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_35');
        if (el) el.textContent = formResult.value;
      });
    }
  }

  // Register Custom Element
  if (typeof window !== 'undefined') {
    customElements.define('app-root', AppComponent);
  }

  // Export for Node.js/testing
  if (typeof module !== 'undefined' && module.exports) {
    module.exports = { AppComponent };
  }

})();