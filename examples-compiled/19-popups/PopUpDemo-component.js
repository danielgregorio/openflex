// Generated by OpenFlex Neo MXML Compiler
// Complete MXML -> Web Components with Full Reactivity

(function() {

  // Generated by OpenFlex Neo
  // AS4 -> JavaScript

  // OpenFlex Reactivity Runtime
  const { Signal, Computed, createEffect } = (typeof window !== 'undefined' && window.OpenFlexRuntime)
    ? window.OpenFlexRuntime
    : require('./runtime/openflex-runtime.js');

  const showSettings = new Signal(false);

  const showConfirm = new Signal(false);

  const lastAction = new Signal('No action yet');

  function openSettings() {
    showSettings.value = true;
  }

  function closeSettings() {
    showSettings.value = false;
    lastAction.value = 'Settings window closed';
  }

  function saveSettings() {
    showSettings.value = false;
    lastAction.value = 'Settings saved successfully!';
  }

  function openConfirm() {
    showConfirm.value = true;
  }

  function confirmYes() {
    showConfirm.value = false;
    lastAction.value = 'Action confirmed - Item deleted';
  }

  function confirmNo() {
    showConfirm.value = false;
    lastAction.value = 'Action cancelled';
  }

  function onAlertClose(button) {
    lastAction.value = 'Alert closed: ' + button;
  }

  function showSimpleAlert() {
    Alert.show('This is a simple informational message.', 'Information', 1, onAlertClose);
  }

  function showYesNoAlert() {
    Alert.show('Do you want to proceed with this action?', 'Confirm', 12, onAlertClose);
  }

  function showOkCancelAlert() {
    Alert.show('Save changes before closing?', 'Save Changes', 3, onAlertClose);
  }


  // OpenFlex PopUp Runtime
  const OpenFlexPopUp = (typeof window !== 'undefined' && window.OpenFlexPopUp)
    ? window.OpenFlexPopUp
    : require('./runtime/openflex-popup.js');

  // Application Component
  class AppComponent extends HTMLElement {
    constructor() {
      super();
      this.attachShadow({ mode: 'open' });
    }

    connectedCallback() {
      this.render();
      this.setupReactivity();
    }

    render() {
      this.shadowRoot.innerHTML = `
        <link rel='stylesheet' href='../runtime/neo-flex-classic-theme.css'>
        <link rel='stylesheet' href='./popup-demo.css'>
      <div id='app_0' class='neo-application'>
        <div id='vbox_1' class='neo-vbox app'>
          <div id='vbox_2' class='neo-vbox wrapper'>
            <div id='vbox_3' class='neo-vbox header'>
              <span id='label_4' class='neo-label title'>Popups and Modals</span>
              <span id='label_5' class='neo-label subtitle'>Modal dialogs for user interaction</span>
            </div>
            <div id='vbox_6' class='neo-vbox card'>
              <div id='hbox_7' class='neo-hbox card-header'>
                <span id='label_8' class='neo-label card-icon'>💬</span>
                <span id='label_9' class='neo-label card-title'>Alert.show()</span>
              </div>
              <div id='hbox_10' class='neo-hbox btn-row'>
                <button id='btn_11' class='neo-button btn btn-secondary'>Info Alert</button>
                <button id='btn_12' class='neo-button btn btn-secondary'>Yes/No Alert</button>
                <button id='btn_13' class='neo-button btn btn-secondary'>Save Alert</button>
              </div>
            </div>
            <div id='vbox_14' class='neo-vbox card'>
              <div id='hbox_15' class='neo-hbox card-header'>
                <span id='label_16' class='neo-label card-icon'>⚙️</span>
                <span id='label_17' class='neo-label card-title'>TitleWindow</span>
              </div>
              <button id='btn_18' class='neo-button btn btn-primary'>Open Settings Window</button>
            </div>
            <div id='vbox_19' class='neo-vbox card'>
              <div id='hbox_20' class='neo-hbox card-header'>
                <span id='label_21' class='neo-label card-icon'>🗑️</span>
                <span id='label_22' class='neo-label card-title'>Confirmation PopUp</span>
              </div>
              <button id='btn_23' class='neo-button btn btn-danger'>Delete Item...</button>
            </div>
            <div id='vbox_24' class='neo-vbox result-box'>
              <span id='label_25' class='neo-label result-label'>LAST ACTION</span>
              <span id='label_26' class='neo-label result-value'></span>
            </div>
          </div>
        </div>
        <div id='titlewindow_27_overlay' class='neo-modal-overlay neo-modal' style='display: none'>
          <div id='titlewindow_27' class='neo-titlewindow'>
            <div class='neo-titlewindow-header'>
              <span class='neo-titlewindow-title'>Settings</span>
              <button id='titlewindow_27_close' class='neo-titlewindow-close'>×</button>
            </div>
            <div class='neo-titlewindow-body'>
              <div id='vbox_28' class='neo-vbox' style='width: 350px; gap: 15px; padding: 20px;'>
                <div id='vbox_29' class='neo-vbox' style='gap: 4px'>
                  <span id='label_30' class='neo-label' style='color: #8e8e93; font-size: 11px; text-transform: uppercase;'>USERNAME</span>
                  <input type='text' id='input_31' class='neo-textinput' placeholder='Enter username...' style='width: 100%' />
                </div>
                <div id='vbox_32' class='neo-vbox' style='gap: 4px'>
                  <span id='label_33' class='neo-label' style='color: #8e8e93; font-size: 11px; text-transform: uppercase;'>THEME</span>
                  <select id='combobox_34' class='neo-combobox' style='width: 100%'>
                    <option>-- Select --</option>
                  </select>
                </div>
                <div id='hbox_35' class='neo-hbox v-align-center' style='gap: 10px'>
                  <input type='checkbox' id='checkbox_36' class='neo-checkbox' />
                  <span id='label_37' class='neo-label' style='color: #fff;'>Enable notifications</span>
                </div>
                <div id='hbox_38' class='neo-hbox h-align-right' style='gap: 10px; margin-top: 10px; padding-top: 15px; border-top: 1px solid #3a3a3c;'>
                  <button id='btn_39' class='neo-button btn btn-primary'>Save</button>
                  <button id='btn_40' class='neo-button btn btn-secondary'>Cancel</button>
                </div>
              </div>
            </div>
          </div>
        </div>
        <div id='popup_41_overlay' class='neo-modal-overlay neo-modal' style='display: none'>
          <div id='popup_41' class='neo-popup'>
            <div id='panel_42' class='neo-panel' style='width: 320px'>
              <div class='neo-panel-header'>Confirm Delete</div>
              <div class='neo-panel-body'>
                <div id='vbox_43' class='neo-vbox' style='gap: 15px; padding: 20px'>
                  <span id='label_44' class='neo-label' style='color: #fff;'>Are you sure you want to delete this item?</span>
                  <span id='label_45' class='neo-label' style='color: #ff3b30; font-size: 12px;'>This action cannot be undone.</span>
                  <div id='hbox_46' class='neo-hbox h-align-right' style='gap: 10px; margin-top: 10px;'>
                    <button id='btn_47' class='neo-button btn btn-danger'>Yes, Delete</button>
                    <button id='btn_48' class='neo-button btn btn-secondary'>Cancel</button>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
      `;

      // Attach event handlers
      this.attachEventHandlers();
    }

    attachEventHandlers() {
      const el_btn_11 = this.shadowRoot.getElementById('btn_11');
      if (el_btn_11) el_btn_11.addEventListener('click', () => showSimpleAlert());
      const el_btn_12 = this.shadowRoot.getElementById('btn_12');
      if (el_btn_12) el_btn_12.addEventListener('click', () => showYesNoAlert());
      const el_btn_13 = this.shadowRoot.getElementById('btn_13');
      if (el_btn_13) el_btn_13.addEventListener('click', () => showOkCancelAlert());
      const el_btn_18 = this.shadowRoot.getElementById('btn_18');
      if (el_btn_18) el_btn_18.addEventListener('click', () => openSettings());
      const el_btn_23 = this.shadowRoot.getElementById('btn_23');
      if (el_btn_23) el_btn_23.addEventListener('click', () => openConfirm());
      const el_titlewindow_27_close = this.shadowRoot.getElementById('titlewindow_27_close');
      if (el_titlewindow_27_close) el_titlewindow_27_close.addEventListener('click', () => { showSettings.value = false; closeSettings(); });
      const el_btn_39 = this.shadowRoot.getElementById('btn_39');
      if (el_btn_39) el_btn_39.addEventListener('click', () => saveSettings());
      const el_btn_40 = this.shadowRoot.getElementById('btn_40');
      if (el_btn_40) el_btn_40.addEventListener('click', () => closeSettings());
      const el_btn_47 = this.shadowRoot.getElementById('btn_47');
      if (el_btn_47) el_btn_47.addEventListener('click', () => confirmYes());
      const el_btn_48 = this.shadowRoot.getElementById('btn_48');
      if (el_btn_48) el_btn_48.addEventListener('click', () => confirmNo());
    }

    setupReactivity() {
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_26');
        if (el) el.textContent = lastAction.value;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('titlewindow_27');
        if (el) el.style.display = showSettings.value ? 'block' : 'none';
      });
      // TitleWindow visibility: titlewindow_27
      createEffect(() => {
        const overlay = this.shadowRoot.getElementById('titlewindow_27_overlay');
        if (!overlay) return;
        overlay.style.display = showSettings.value ? 'flex' : 'none';
      });
      // ComboBox: combobox_34
      createEffect(() => {
        const el = this.shadowRoot.getElementById('combobox_34');
        if (!el) return;
      
        // Limpar opções anteriores (exceto primeira)
        while (el.options.length > 1) el.remove(1);
      
        // Renderizar opções do array
        const items = [{label:'Light'}, {label:'Dark'}, {label:'System'}] || [];
        items.forEach((item, index) => {
          const option = document.createElement('option');
          option.value = index;
          option.textContent = item.label || item || '';
          el.appendChild(option);
        });
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('popup_41');
        if (el) el.style.display = showConfirm.value ? 'block' : 'none';
      });
      // PopUp visibility: popup_41
      createEffect(() => {
        const overlay = this.shadowRoot.getElementById('popup_41_overlay');
        if (!overlay) return;
        overlay.style.display = showConfirm.value ? 'flex' : 'none';
      });
    }
  }

  // Register Custom Element
  if (typeof window !== 'undefined') {
    customElements.define('app-root', AppComponent);
  }

  // Export for Node.js/testing
  if (typeof module !== 'undefined' && module.exports) {
    module.exports = { AppComponent };
  }

})();