// Generated by OpenFlex Neo MXML Compiler
// Complete MXML -> Web Components with Full Reactivity

(function() {

  // Generated by OpenFlex Neo
  // AS4 -> JavaScript

  // OpenFlex Reactivity Runtime
  const { Signal, Computed, createEffect } = (typeof window !== 'undefined' && window.OpenFlexRuntime)
    ? window.OpenFlexRuntime
    : require('./runtime/openflex-runtime.js');

  const selectedDate = new Signal(new Date());

  const startDate = new Signal(new Date());

  const endDate = new Signal(new Date());

  const formattedDate = new Computed(() => {
    return selectedDate.value.toLocaleDateString('en-US', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' });
  });

  const formattedStart = new Computed(() => {
    return startDate.value.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' });
  });

  const formattedEnd = new Computed(() => {
    return endDate.value.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' });
  });

  function onDateChange(event) {
    selectedDate.value = event.date;
  }


  // OpenFlex Calendar Runtime
  const OpenFlexCalendar = (typeof window !== 'undefined' && window.OpenFlexCalendar)
    ? window.OpenFlexCalendar
    : require('./runtime/openflex-calendar.js');

  // Application Component
  class AppComponent extends HTMLElement {
    constructor() {
      super();
      this.attachShadow({ mode: 'open' });
    }

    connectedCallback() {
      this.render();
      this.setupReactivity();
    }

    render() {
      this.shadowRoot.innerHTML = `
        <link rel='stylesheet' href='../runtime/neo-flex-classic-theme.css'>
        <link rel='stylesheet' href='./calendar-demo.css'>
      <div id='app_0' class='neo-application'>
        <div id='vbox_1' class='neo-vbox app'>
          <div id='vbox_2' class='neo-vbox wrapper'>
            <div id='vbox_3' class='neo-vbox header'>
              <span id='label_4' class='neo-label title'>Calendar and DatePicker</span>
              <span id='label_5' class='neo-label subtitle'>Date selection with flexible formatting</span>
            </div>
            <div id='hbox_6' class='neo-hbox grid'>
              <div id='vbox_7' class='neo-vbox card'>
                <div id='hbox_8' class='neo-hbox card-header'>
                  <span id='label_9' class='neo-label card-icon'>📅</span>
                  <span id='label_10' class='neo-label card-title'>Calendar</span>
                </div>
                <div id='vbox_11' class='neo-vbox calendar-container'>
                  <div id='calendar_12' class='neo-calendar'>
                    <!-- Calendar rendered by OpenFlexCalendar -->
                  </div>
                </div>
                <div id='hbox_13' class='neo-hbox selected-date-display'>
                  <span id='label_14' class='neo-label date-pin'>📌</span>
                  <span id='label_15' class='neo-label date-text'></span>
                </div>
              </div>
              <div id='vbox_16' class='neo-vbox card'>
                <div id='hbox_17' class='neo-hbox card-header'>
                  <span id='label_18' class='neo-label card-icon'>📆</span>
                  <span id='label_19' class='neo-label card-title'>DatePicker</span>
                </div>
                <div id='vbox_20' class='neo-vbox field-group'>
                  <span id='label_21' class='neo-label field-label'>SINGLE DATE</span>
                  <div id='datefield_22' class='neo-datefield' style='width: 100%'>
                    <input type='text' id='datefield_22_input' class='neo-datefield-input' readonly placeholder='Select date...' />
                    <button id='datefield_22_btn' class='neo-datefield-btn'>📅</button>
                    <div id='datefield_22_popup' class='neo-datefield-popup' style='display: none'>
                      <!-- Calendar popup rendered by OpenFlexCalendar -->
                    </div>
                  </div>
                </div>
                <div id='vbox_23' class='neo-vbox field-group'>
                  <span id='label_24' class='neo-label field-label'>DATE RANGE</span>
                  <div id='hbox_25' class='neo-hbox date-range-row'>
                    <div id='vbox_26' class='neo-vbox date-field-wrapper'>
                      <span id='label_27' class='neo-label field-label'>From</span>
                      <div id='datefield_28' class='neo-datefield' style='width: 100%'>
                        <input type='text' id='datefield_28_input' class='neo-datefield-input' readonly placeholder='Select date...' />
                        <button id='datefield_28_btn' class='neo-datefield-btn'>📅</button>
                        <div id='datefield_28_popup' class='neo-datefield-popup' style='display: none'>
                          <!-- Calendar popup rendered by OpenFlexCalendar -->
                        </div>
                      </div>
                    </div>
                    <span id='label_29' class='neo-label range-arrow'>→</span>
                    <div id='vbox_30' class='neo-vbox date-field-wrapper'>
                      <span id='label_31' class='neo-label field-label'>To</span>
                      <div id='datefield_32' class='neo-datefield' style='width: 100%'>
                        <input type='text' id='datefield_32_input' class='neo-datefield-input' readonly placeholder='Select date...' />
                        <button id='datefield_32_btn' class='neo-datefield-btn'>📅</button>
                        <div id='datefield_32_popup' class='neo-datefield-popup' style='display: none'>
                          <!-- Calendar popup rendered by OpenFlexCalendar -->
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
                <div id='vbox_33' class='neo-vbox range-display'>
                  <span id='label_34' class='neo-label range-label'>Selected Range</span>
                  <span id='label_35' class='neo-label range-value'></span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
      `;

      // Attach event handlers
      this.attachEventHandlers();
    }

    attachEventHandlers() {
    }

    setupReactivity() {
      // Calendar: calendar_12
      const calConfig_calendar_12 = {"minDate": "", "maxDate": "", "showWeekNumbers": false, "showToday": true, "changeHandler": "onDateChange"};
      createEffect(() => {
        const container = this.shadowRoot.getElementById('calendar_12');
        if (!container) return;
        const dateValue = selectedDate.value;
        if (typeof OpenFlexCalendar !== 'undefined') {
          OpenFlexCalendar.renderCalendar(container, {
            ...calConfig_calendar_12,
            selectedDate: dateValue,
            onSelect: (date) => { selectedDate.value = date; }
          });
        }
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_15');
        if (el) el.textContent = formattedDate.value;
      });
      // DateField: datefield_22
      const dfConfig_datefield_22 = {"formatString": "yyyy-MM-dd", "minDate": "", "maxDate": "", "changeHandler": ""};
      createEffect(() => {
        const container = this.shadowRoot.getElementById('datefield_22');
        const input = this.shadowRoot.getElementById('datefield_22_input');
        if (!container || !input) return;
        const dateValue = selectedDate.value;
        if (dateValue) {
          input.value = typeof OpenFlexCalendar !== 'undefined' 
            ? OpenFlexCalendar.formatDate(dateValue, dfConfig_datefield_22.formatString)
            : dateValue.toISOString().split('T')[0];
        }
      });
    
      // DateField popup toggle datefield_22
      const dfBtn_datefield_22 = this.shadowRoot.getElementById('datefield_22_btn');
      const dfPopup_datefield_22 = this.shadowRoot.getElementById('datefield_22_popup');
      if (dfBtn_datefield_22 && dfPopup_datefield_22) {
        dfBtn_datefield_22.addEventListener('click', () => {
          const isOpen = dfPopup_datefield_22.style.display !== 'none';
          dfPopup_datefield_22.style.display = isOpen ? 'none' : 'block';
          if (!isOpen && typeof OpenFlexCalendar !== 'undefined') {
            OpenFlexCalendar.renderCalendar(dfPopup_datefield_22, {
              ...dfConfig_datefield_22,
              selectedDate: selectedDate.value,
              onSelect: (date) => {
                selectedDate.value = date;
                dfPopup_datefield_22.style.display = 'none';
              }
            });
          }
        });
      }
      // DateField: datefield_28
      const dfConfig_datefield_28 = {"formatString": "yyyy-MM-dd", "minDate": "", "maxDate": "", "changeHandler": ""};
      createEffect(() => {
        const container = this.shadowRoot.getElementById('datefield_28');
        const input = this.shadowRoot.getElementById('datefield_28_input');
        if (!container || !input) return;
        const dateValue = startDate.value;
        if (dateValue) {
          input.value = typeof OpenFlexCalendar !== 'undefined' 
            ? OpenFlexCalendar.formatDate(dateValue, dfConfig_datefield_28.formatString)
            : dateValue.toISOString().split('T')[0];
        }
      });
    
      // DateField popup toggle datefield_28
      const dfBtn_datefield_28 = this.shadowRoot.getElementById('datefield_28_btn');
      const dfPopup_datefield_28 = this.shadowRoot.getElementById('datefield_28_popup');
      if (dfBtn_datefield_28 && dfPopup_datefield_28) {
        dfBtn_datefield_28.addEventListener('click', () => {
          const isOpen = dfPopup_datefield_28.style.display !== 'none';
          dfPopup_datefield_28.style.display = isOpen ? 'none' : 'block';
          if (!isOpen && typeof OpenFlexCalendar !== 'undefined') {
            OpenFlexCalendar.renderCalendar(dfPopup_datefield_28, {
              ...dfConfig_datefield_28,
              selectedDate: startDate.value,
              onSelect: (date) => {
                startDate.value = date;
                dfPopup_datefield_28.style.display = 'none';
              }
            });
          }
        });
      }
      // DateField: datefield_32
      const dfConfig_datefield_32 = {"formatString": "yyyy-MM-dd", "minDate": "", "maxDate": "", "changeHandler": ""};
      createEffect(() => {
        const container = this.shadowRoot.getElementById('datefield_32');
        const input = this.shadowRoot.getElementById('datefield_32_input');
        if (!container || !input) return;
        const dateValue = endDate.value;
        if (dateValue) {
          input.value = typeof OpenFlexCalendar !== 'undefined' 
            ? OpenFlexCalendar.formatDate(dateValue, dfConfig_datefield_32.formatString)
            : dateValue.toISOString().split('T')[0];
        }
      });
    
      // DateField popup toggle datefield_32
      const dfBtn_datefield_32 = this.shadowRoot.getElementById('datefield_32_btn');
      const dfPopup_datefield_32 = this.shadowRoot.getElementById('datefield_32_popup');
      if (dfBtn_datefield_32 && dfPopup_datefield_32) {
        dfBtn_datefield_32.addEventListener('click', () => {
          const isOpen = dfPopup_datefield_32.style.display !== 'none';
          dfPopup_datefield_32.style.display = isOpen ? 'none' : 'block';
          if (!isOpen && typeof OpenFlexCalendar !== 'undefined') {
            OpenFlexCalendar.renderCalendar(dfPopup_datefield_32, {
              ...dfConfig_datefield_32,
              selectedDate: endDate.value,
              onSelect: (date) => {
                endDate.value = date;
                dfPopup_datefield_32.style.display = 'none';
              }
            });
          }
        });
      }
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_35');
        if (el) el.textContent = `${formattedStart.value} → {formattedEnd}`;
      });
    }
  }

  // Register Custom Element
  if (typeof window !== 'undefined') {
    customElements.define('app-root', AppComponent);
  }

  // Export for Node.js/testing
  if (typeof module !== 'undefined' && module.exports) {
    module.exports = { AppComponent };
  }

})();