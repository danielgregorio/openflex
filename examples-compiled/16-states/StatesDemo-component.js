// Generated by OpenFlex Neo MXML Compiler
// Complete MXML -> Web Components with Full Reactivity

(function() {

  // Generated by OpenFlex Neo
  // AS4 -> JavaScript

  // OpenFlex Reactivity Runtime
  const { Signal, Computed, createEffect } = (typeof window !== 'undefined' && window.OpenFlexRuntime)
    ? window.OpenFlexRuntime
    : require('./runtime/openflex-runtime.js');

  const currentState = new Signal('view');

  const userName = new Signal('John Doe');

  const userEmail = new Signal('john@example.com');

  const userBio = new Signal('Software developer passionate about clean code and great user experiences.');

  function goToEdit() {
    currentState.value = 'edit';
  }

  function goToView() {
    currentState.value = 'view';
  }

  function saveChanges() {
    currentState.value = 'view';
  }

  const isViewMode = new Computed(() => {
    return currentState.value === 'view';
  });

  const isEditMode = new Computed(() => {
    return currentState.value === 'edit';
  });

  const stateLabel = new Computed(() => {
    return (currentState.value === 'view' ? 'VIEW MODE' : 'EDIT MODE');
  });

  const stateBadgeClass = new Computed(() => {
    return (currentState.value === 'view' ? 'badge-green' : 'badge-orange');
  });


  // Application Component
  class AppComponent extends HTMLElement {
    constructor() {
      super();
      this.attachShadow({ mode: 'open' });
    }

    connectedCallback() {
      this.render();
      this.setupReactivity();
    }

    render() {
      this.shadowRoot.innerHTML = `
        <link rel='stylesheet' href='../runtime/neo-flex-classic-theme.css'>
        <link rel='stylesheet' href='./states-demo.css'>
      <div id='app_0' class='neo-application'>
        <div id='vbox_1' class='neo-vbox app'>
          <div id='vbox_2' class='neo-vbox wrapper'>
            <div id='vbox_3' class='neo-vbox header'>
              <span id='label_4' class='neo-label title'>States and Transitions</span>
              <span id='label_5' class='neo-label subtitle'>Dynamic UI with view and edit modes</span>
            </div>
            <div id='vbox_6' class='neo-vbox card'>
              <div id='hbox_7' class='neo-hbox card-header'>
                <div id='hbox_8' class='neo-hbox card-title-row'>
                  <span id='label_9' class='neo-label card-icon'>👤</span>
                  <span id='label_10' class='neo-label card-title'>User Profile</span>
                </div>
                <span id='label_11' class='neo-label'></span>
              </div>
              <div id='vbox_12' class='neo-vbox view-content'>
                <div id='vbox_13' class='neo-vbox field-group'>
                  <span id='label_14' class='neo-label field-label'>NAME</span>
                  <span id='label_15' class='neo-label field-value'></span>
                </div>
                <div id='vbox_16' class='neo-vbox field-group'>
                  <span id='label_17' class='neo-label field-label'>EMAIL</span>
                  <span id='label_18' class='neo-label field-value'></span>
                </div>
                <div id='vbox_19' class='neo-vbox field-group'>
                  <span id='label_20' class='neo-label field-label'>BIO</span>
                  <span id='label_21' class='neo-label field-value'></span>
                </div>
                <div id='hbox_22' class='neo-hbox btn-row'>
                  <button id='btn_23' class='neo-button btn btn-primary'>✏️ Edit Profile</button>
                </div>
              </div>
              <div id='vbox_24' class='neo-vbox edit-content'>
                <div id='vbox_25' class='neo-vbox field-group'>
                  <span id='label_26' class='neo-label field-label'>NAME</span>
                  <input type='text' id='input_27' class='neo-textinput field-input' placeholder='' />
                </div>
                <div id='vbox_28' class='neo-vbox field-group'>
                  <span id='label_29' class='neo-label field-label'>EMAIL</span>
                  <input type='text' id='input_30' class='neo-textinput field-input' placeholder='' />
                </div>
                <div id='vbox_31' class='neo-vbox field-group'>
                  <span id='label_32' class='neo-label field-label'>BIO</span>
                </div>
                <div id='hbox_33' class='neo-hbox btn-row'>
                  <button id='btn_34' class='neo-button btn btn-success'>💾 Save</button>
                  <button id='btn_35' class='neo-button btn btn-secondary'>Cancel</button>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
      `;

      // Attach event handlers
      this.attachEventHandlers();
    }

    attachEventHandlers() {
      const el_btn_23 = this.shadowRoot.getElementById('btn_23');
      if (el_btn_23) el_btn_23.addEventListener('click', () => goToEdit());
      const el_btn_34 = this.shadowRoot.getElementById('btn_34');
      if (el_btn_34) el_btn_34.addEventListener('click', () => saveChanges());
      const el_btn_35 = this.shadowRoot.getElementById('btn_35');
      if (el_btn_35) el_btn_35.addEventListener('click', () => goToView());
    }

    setupReactivity() {
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_11');
        if (el) el.className = `badge ${stateBadgeClass.value}`;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_11');
        if (el) el.textContent = stateLabel.value;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('vbox_12');
        if (el) el.style.display = isViewMode.value ? 'block' : 'none';
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_15');
        if (el) el.textContent = userName.value;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_18');
        if (el) el.textContent = userEmail.value;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_21');
        if (el) el.textContent = userBio.value;
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('vbox_24');
        if (el) el.style.display = isEditMode.value ? 'block' : 'none';
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('input_27');
        if (el) el.value = userName.value;
      });
    
      // Two-way binding para input_27
      const el_input_27 = this.shadowRoot.getElementById('input_27');
      if (el_input_27) {
        el_input_27.addEventListener('input', (e) => {
          userName.value = e.target.value;
        });
      }
      createEffect(() => {
        const el = this.shadowRoot.getElementById('input_30');
        if (el) el.value = userEmail.value;
      });
    
      // Two-way binding para input_30
      const el_input_30 = this.shadowRoot.getElementById('input_30');
      if (el_input_30) {
        el_input_30.addEventListener('input', (e) => {
          userEmail.value = e.target.value;
        });
      }
    }
  }

  // Register Custom Element
  if (typeof window !== 'undefined') {
    customElements.define('app-root', AppComponent);
  }

  // Export for Node.js/testing
  if (typeof module !== 'undefined' && module.exports) {
    module.exports = { AppComponent };
  }

})();