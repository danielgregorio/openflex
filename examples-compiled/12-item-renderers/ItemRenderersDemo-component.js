// Generated by OpenFlex Neo MXML Compiler
// Complete MXML -> Web Components with Full Reactivity

(function() {

  // Generated by OpenFlex Neo
  // AS4 -> JavaScript

  // OpenFlex Reactivity Runtime
  const { Signal, Computed, createEffect } = (typeof window !== 'undefined' && window.OpenFlexRuntime)
    ? window.OpenFlexRuntime
    : require('./runtime/openflex-runtime.js');

  const products = new Signal([]);

  const employees = new Signal([]);

  const selectedProduct = new Signal(-1);

  const selectedEmployee = new Signal(-1);

  async function loadExternalData() {
    try {
      const response = await fetch('./item-renderers-demo-data.json');
      const data = await response.json();
      products.value = data.products;
      employees.value = data.employees;
    } catch (error) {
      console.error('Error loading data:', error);
    }
  }

  function selectProduct(index) {
    selectedProduct.value = index;
  }

  function selectEmployee(index) {
    selectedEmployee.value = index;
  }

  function formatPrice(amount) {
    return '$' + amount.toFixed(2);
  }

  function formatSalary(amount) {
    return '$' + amount.toLocaleString();
  }

  function formatDate(dateStr) {
    let date = new Date(dateStr);
    return date.toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' });
  }

  function formatTenure(dateStr) {
    let hireDate = new Date(dateStr);
    let today = new Date();
    let years = Math.floor((today - hireDate) / (365.25 * 24 * 60 * 60 * 1000));
    return years + (years === 1 ? ' year' : ' years');
  }

  function getRatingStars(rating) {
    let stars = Math.round(rating);
    let result = '';
    for (let i = 0; i < 5; i++) {
      (result += i < stars ? '★' : '☆');
    }
    return result;
  }

  function getDeptColor(dept) {
    if (dept === 'Engineering')   return '#5856d6';
    if (dept === 'Marketing')   return '#ff9500';
    if (dept === 'Sales')   return '#34c759';
    return '#8e8e93';
  }

  const selectedProductInfo = new Computed(() => {
    if (selectedProduct.value >= 0 && products.value.length > 0) {
      return products.value[selectedProduct.value].name + ' - ' + formatPrice(products.value[selectedProduct.value].price);
    }
    return 'None selected';
  });

  const selectedEmployeeInfo = new Computed(() => {
    if (selectedEmployee.value >= 0 && employees.value.length > 0) {
      return employees.value[selectedEmployee.value].name + ' - ' + employees.value[selectedEmployee.value].department;
    }
    return 'None selected';
  });


  // Application Component
  class AppComponent extends HTMLElement {
    constructor() {
      super();
      this.attachShadow({ mode: 'open' });
    }

    connectedCallback() {
      this.render();
      loadExternalData().then(() => {
        this.setupReactivity();
      });
    }

    render() {
      this.shadowRoot.innerHTML = `
        <link rel='stylesheet' href='../runtime/neo-flex-classic-theme.css'>
        <link rel='stylesheet' href='../runtime/openflex-ios-theme.css'>
        <link rel='stylesheet' href='./item-renderers-demo.css'>
      <div id='app_0' class='neo-application'>
        <div id='vbox_1' class='neo-vbox app'>
          <div id='vbox_2' class='neo-vbox wrapper'>
            <div id='vbox_3' class='neo-vbox header'>
              <span id='label_4' class='neo-label title'>Item Renderers</span>
              <span id='label_5' class='neo-label subtitle'>Custom data formatting with labelFunction</span>
            </div>
            <div id='hbox_6' class='neo-hbox info-banner'>
              <span id='label_7' class='neo-label info-icon'>💡</span>
              <span id='label_8' class='neo-label info-text'>The labelFunction attribute allows custom formatting of data in List and DataGrid components.</span>
            </div>
            <div id='hbox_9' class='neo-hbox grid'>
              <div id='vbox_10' class='neo-vbox card'>
                <div id='vbox_11' class='neo-vbox card-header'>
                  <span id='label_12' class='neo-label card-title'>Products List</span>
                  <span id='label_13' class='neo-label card-subtitle'>Custom item rendering with price, stock and rating</span>
                </div>
                <div id='vbox_14' class='neo-vbox product-list'>
                  <div id='repeater_15' class='neo-repeater'>
                    <!-- Repeater content will be dynamically generated -->
                  </div>
                </div>
                <div id='hbox_16' class='neo-hbox selection-info'>
                  <span id='label_17' class='neo-label selection-label'>Selected:</span>
                  <span id='label_18' class='neo-label selection-value'></span>
                </div>
              </div>
              <div id='vbox_19' class='neo-vbox card'>
                <div id='vbox_20' class='neo-vbox card-header'>
                  <span id='label_21' class='neo-label card-title'>Products DataGrid</span>
                  <span id='label_22' class='neo-label card-subtitle'>Column-based formatting with labelFunction</span>
                </div>
                <div id='vbox_23' class='neo-vbox datagrid'>
                  <div id='hbox_24' class='neo-hbox datagrid-header'>
                    <span id='label_25' class='neo-label datagrid-header-cell'>Product</span>
                    <span id='label_26' class='neo-label datagrid-header-cell'>Price</span>
                    <span id='label_27' class='neo-label datagrid-header-cell'>Stock</span>
                    <span id='label_28' class='neo-label datagrid-header-cell'>Rating</span>
                  </div>
                  <div id='repeater_29' class='neo-repeater'>
                    <!-- Repeater content will be dynamically generated -->
                  </div>
                </div>
              </div>
              <div id='vbox_30' class='neo-vbox card full-card'>
                <div id='vbox_31' class='neo-vbox card-header'>
                  <span id='label_32' class='neo-label card-title'>Employees DataGrid</span>
                  <span id='label_33' class='neo-label card-subtitle'>Date formatting and computed tenure column</span>
                </div>
                <div id='vbox_34' class='neo-vbox datagrid'>
                  <div id='hbox_35' class='neo-hbox datagrid-header'>
                    <span id='label_36' class='neo-label datagrid-header-cell'>Name</span>
                    <span id='label_37' class='neo-label datagrid-header-cell'>Department</span>
                    <span id='label_38' class='neo-label datagrid-header-cell'>Salary</span>
                    <span id='label_39' class='neo-label datagrid-header-cell'>Hire Date</span>
                    <span id='label_40' class='neo-label datagrid-header-cell'>Tenure</span>
                  </div>
                  <div id='repeater_41' class='neo-repeater'>
                    <!-- Repeater content will be dynamically generated -->
                  </div>
                </div>
                <div id='hbox_42' class='neo-hbox selection-info'>
                  <span id='label_43' class='neo-label selection-label'>Selected:</span>
                  <span id='label_44' class='neo-label selection-value'></span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
      `;

      // Attach event handlers
      this.attachEventHandlers();
    }

    attachEventHandlers() {
    }

    setupReactivity() {
      // Repeater: repeater_15
      createEffect(() => {
        const el = this.shadowRoot.getElementById('repeater_15');
        if (!el) return;

        // Limpar conteudo anterior
        el.innerHTML = '';

        // Renderizar itens do array
        const items = products.value || [];
        items.forEach((item, index) => {
          const wrapper = document.createElement('div');
          wrapper.innerHTML = `<div class="neo-hbox product-item ${index === selectedProduct.value ? 'selected' : ''}">
  <span class="neo-label product-emoji">${item.emoji}</span>
  <div class="neo-vbox product-info">
    <span class="neo-label product-name">${item.name}</span>
    <div class="neo-hbox product-details">
      <span class="neo-label product-price">${formatPrice(item.price)}</span>
      <span class="neo-label product-rating">${getRatingStars(item.rating)}</span>
    </div>
  </div>
  <span class="neo-label stock-badge ${item.inStock ? 'in-stock' : 'out-of-stock'}">${item.inStock ? 'In Stock' : 'Out'}</span>
</div>`;
          if (wrapper.firstElementChild) {
            wrapper.firstElementChild.addEventListener('click', () => selectProduct(index));
            el.appendChild(wrapper.firstElementChild);
          }
        });
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_18');
        if (el) el.textContent = selectedProductInfo.value;
      });
      // Repeater: repeater_29
      createEffect(() => {
        const el = this.shadowRoot.getElementById('repeater_29');
        if (!el) return;

        // Limpar conteudo anterior
        el.innerHTML = '';

        // Renderizar itens do array
        const items = products.value || [];
        items.forEach((item, index) => {
          const wrapper = document.createElement('div');
          wrapper.innerHTML = `<div class="neo-hbox datagrid-row ${index === selectedProduct.value ? 'selected' : ''}">
  <span class="neo-label datagrid-cell cell-name">${item.name}</span>
  <span class="neo-label datagrid-cell" style="color: #34c759;">${formatPrice(item.price)}</span>
  <span class="neo-label datagrid-cell" style="color: ${item.inStock ? '#34c759' : '#ff3b30'};">${item.inStock ? 'Yes' : 'No'}</span>
  <span class="neo-label datagrid-cell" style="color: #ff9500;">${getRatingStars(item.rating)}</span>
</div>`;
          if (wrapper.firstElementChild) {
            wrapper.firstElementChild.addEventListener('click', () => selectProduct(index));
            el.appendChild(wrapper.firstElementChild);
          }
        });
      });
      // Repeater: repeater_41
      createEffect(() => {
        const el = this.shadowRoot.getElementById('repeater_41');
        if (!el) return;

        // Limpar conteudo anterior
        el.innerHTML = '';

        // Renderizar itens do array
        const items = employees.value || [];
        items.forEach((item, index) => {
          const wrapper = document.createElement('div');
          wrapper.innerHTML = `<div class="neo-hbox datagrid-row ${index === selectedEmployee.value ? 'selected' : ''}">
  <span class="neo-label datagrid-cell cell-name">${item.name}</span>
  <div class="neo-hbox datagrid-cell">
    <span class="neo-label dept-badge" style="background: ${getDeptColor(item.department)}20; color: ${getDeptColor(item.department)};">${item.department}</span>
  </div>
  <span class="neo-label datagrid-cell cell-salary">${formatSalary(item.salary)}</span>
  <span class="neo-label datagrid-cell">${formatDate(item.hireDate)}</span>
  <span class="neo-label datagrid-cell cell-tenure">${formatTenure(item.hireDate)}</span>
</div>`;
          if (wrapper.firstElementChild) {
            wrapper.firstElementChild.addEventListener('click', () => selectEmployee(index));
            el.appendChild(wrapper.firstElementChild);
          }
        });
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_44');
        if (el) el.textContent = selectedEmployeeInfo.value;
      });
    }
  }

  // Register Custom Element
  if (typeof window !== 'undefined') {
    customElements.define('app-root', AppComponent);
  }

  // Export for Node.js/testing
  if (typeof module !== 'undefined' && module.exports) {
    module.exports = { AppComponent };
  }

})();
