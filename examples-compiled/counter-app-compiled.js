// Generated by OpenFlex Neo MXML Compiler
// Complete MXML -> Web Components with Full Reactivity

// Generated by OpenFlex Neo
// AS4 -> JavaScript

// OpenFlex Reactivity Runtime
const { Signal, Computed, createEffect } = (typeof window !== 'undefined' && window.OpenFlexRuntime)
  ? window.OpenFlexRuntime
  : require('./runtime/openflex-runtime.js');

const count = new Signal(0);

const doubled = new Computed(() => {
  return count.value * 2;
});

const isEven = new Computed(() => {
  return count.value % 2 === 0;
});

function increment() {
  count.value++;
}

function decrement() {
  count.value--;
}

function reset() {
  count.value = 0;
}


// Application Component
class AppComponent extends HTMLElement {
  constructor() {
    super();
    this.attachShadow({ mode: 'open' });
  }

  connectedCallback() {
    this.render();
    this.setupReactivity();
  }

  render() {
    this.shadowRoot.innerHTML = `
      <style>.counter-display {
            font-size: 24px;
            font-weight: bold;
            color: #0066cc;
        }

        .info-text {
            font-size: 14px;
            color: #666;
        }</style>
    <div id='app_0' class='neo-application'>
      <div id='vbox_1' class='neo-vbox h-align-center v-align-middle' style='width: 100%; height: 100%'>
        <div id='panel_2' class='neo-panel'>
          <div class='neo-panel-header'>Counter Demo - OpenFlex Neo</div>
          <div class='neo-panel-body'>
            <div id='vbox_3' class='neo-vbox'>
              <div id='hbox_4' class='neo-hbox h-align-center'>
                <span id='label_5' class='neo-label'>Count:</span>
                <span id='label_6' class='counter-display'></span>
              </div>
              <div id='vbox_7' class='neo-vbox'>
                <span id='label_8' class='info-text'></span>
                <span id='label_9' class='info-text'></span>
              </div>
              <div id='hbox_10' class='neo-hbox h-align-center'>
                <button id='btn_11' class='neo-button'>−</button>
                <button id='btn_12' class='neo-button'>Reset</button>
                <button id='btn_13' class='neo-button'>+</button>
              </div>
              <span id='label_14' class='neo-label' style='color: #999'>Built with Neo 🆕</span>
            </div>
          </div>
        </div>
      </div>
    </div>
    `;

    // Attach event handlers
    this.attachEventHandlers();
  }

  attachEventHandlers() {
    const el_btn_11 = this.shadowRoot.getElementById('btn_11');
    if (el_btn_11) el_btn_11.addEventListener('click', () => decrement());
    const el_btn_12 = this.shadowRoot.getElementById('btn_12');
    if (el_btn_12) el_btn_12.addEventListener('click', () => reset());
    const el_btn_13 = this.shadowRoot.getElementById('btn_13');
    if (el_btn_13) el_btn_13.addEventListener('click', () => increment());
  }

  setupReactivity() {
    createEffect(() => {
      const el = this.shadowRoot.getElementById('label_6');
      if (el) el.textContent = count.value;
    });
    createEffect(() => {
      const el = this.shadowRoot.getElementById('label_8');
      if (el) el.textContent = `Doubled: ${doubled.value}`;
    });
    createEffect(() => {
      const el = this.shadowRoot.getElementById('label_9');
      if (el) el.textContent = `Is Even: ${isEven.value}`;
    });
  }
}

// Register Custom Element
if (typeof window !== 'undefined') {
  customElements.define('app-root', AppComponent);
}

// Export for Node.js/testing
if (typeof module !== 'undefined' && module.exports) {
  module.exports = { AppComponent };
}