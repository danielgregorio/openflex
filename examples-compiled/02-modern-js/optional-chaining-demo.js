// Generated by OpenFlex Neo - Optional Chaining Demo (Simplified)
// AS4 -> JavaScript

// Polyfill for Node.js
const trace = typeof console !== 'undefined' ? console.log : () => {};

trace("=== Optional Chaining Demo ===");
trace("");

// Safe property access
trace("--- Safe Property Access ---");
const user = {
    name: "Alice",
    address: {
        city: "Paris",
        zip: "75001"
    }
};

trace("User city:", user?.address?.city);
trace("User country:", user?.address?.country ?? "Not specified");
trace("");

// Null/undefined handling
trace("--- Null/Undefined Handling ---");
const emptyUser = null;
trace("Empty user name:", emptyUser?.name ?? "Anonymous");
trace("");

// Array access
trace("--- Optional Array Access ---");
const users = [
    { name: "Alice" },
    { name: "Bob" }
];
trace("First user:", users?.[0]?.name);
trace("Third user:", users?.[2]?.name ?? "Doesn't exist");
trace("");

// Function calls
trace("--- Optional Function Calls ---");
const obj = {
    greet: () => "Hello!"
};
trace("Greet:", obj.greet?.());
trace("Missing method:", obj.missing?.() ?? "Method doesn't exist");
