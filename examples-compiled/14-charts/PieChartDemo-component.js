// Generated by OpenFlex Neo MXML Compiler
// Complete MXML -> Web Components with Full Reactivity

(function() {

  // Generated by OpenFlex Neo
  // AS4 -> JavaScript

  // OpenFlex Reactivity Runtime
  const { Signal, Computed, createEffect } = (typeof window !== 'undefined' && window.OpenFlexRuntime)
    ? window.OpenFlexRuntime
    : require('./runtime/openflex-runtime.js');

  const browserShare = new Signal([]);

  async function loadExternalData() {
    try {
      const response = await fetch('./pie-chart-demo-data.json');
      const data = await response.json();
      browserShare.value = data.browserShare;
    } catch (error) {
      console.error('Error loading data:', error);
    }
  }

  function randomizeData() {
    const values = [Math.floor(Math.random() * 30) + 50, Math.floor(Math.random() * 15) + 10, Math.floor(Math.random() * 10) + 5, Math.floor(Math.random() * 8) + 3, Math.floor(Math.random() * 5) + 2];
    const total = values.reduce((a, b) => a + b, 0);
    browserShare.value = browserShare.value.map((d, i) => ({ ...d, value: Math.round(values[i] / total * 100) }));
  }


  // OpenFlex Charts Runtime (ECharts wrapper)
  const OpenFlexCharts = (typeof window !== 'undefined' && window.OpenFlexCharts)
    ? window.OpenFlexCharts
    : require('./runtime/openflex-charts.js');

  // Application Component
  class AppComponent extends HTMLElement {
    constructor() {
      super();
      this.attachShadow({ mode: 'open' });
    }

    connectedCallback() {
      this.render();
      loadExternalData().then(() => {
        this.setupReactivity();
      });
    }

    render() {
      this.shadowRoot.innerHTML = `
        <style>@import url('../runtime/neo-flex-classic-theme.css');</style>
      <div id='app_0' class='neo-application'>
        <div id='panel_1' class='neo-panel'>
          <div class='neo-panel-header'>PieChart - Grafico de Pizza</div>
          <div class='neo-panel-body'>
            <div id='vbox_2' class='neo-vbox' style='width: 100%; gap: 15px'>
              <span id='label_3' class='neo-label'>O PieChart exibe dados como fatias de um circulo, ideal para mostrar proporcoes e partes de um todo.</span>
              <div id='hbox_4' class='neo-hbox h-align-right' style='width: 100%'>
                <button id='btn_5' class='neo-button'>Atualizar Dados</button>
              </div>
              <div id='hbox_6' class='neo-hbox h-align-center' style='width: 100%'>
                <div id='chart_7' class='neo-chart' style='width: 500px; height: 350px'>
                  <!-- Chart rendered by ECharts -->
                </div>
              </div>
              <div id='panel_8' class='neo-panel'>
                <div class='neo-panel-header'>Codigo Fonte</div>
                <div class='neo-panel-body'>
                  <div id='vbox_9' class='neo-vbox' style='gap: 5px'>
                    <span id='label_10' class='neo-label'></span>
                    <span id='label_11' class='neo-label'>    <series></span>
                    <span id='label_12' class='neo-label'>        <PieSeries field='value' nameField='name' displayName='Market Share' /></span>
                    <span id='label_13' class='neo-label'>    </series></span>
                    <span id='label_14' class='neo-label'></PieChart></span>
                  </div>
                </div>
              </div>
              <div id='panel_15' class='neo-panel'>
                <div class='neo-panel-header'>Dados</div>
                <div class='neo-panel-body'>
                  <div id='vbox_16' class='neo-vbox' style='gap: 5px'>
                    <span id='label_17' class='neo-label'>@reactive var browserShare: Array = [</span>
                    <span id='label_18' class='neo-label'></span>
                    <span id='label_19' class='neo-label'></span>
                    <span id='label_20' class='neo-label'></span>
                    <span id='label_21' class='neo-label'></span>
                    <span id='label_22' class='neo-label'></span>
                    <span id='label_23' class='neo-label'>];</span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
      `;

      // Attach event handlers
      this.attachEventHandlers();
    }

    attachEventHandlers() {
      const el_btn_5 = this.shadowRoot.getElementById('btn_5');
      if (el_btn_5) el_btn_5.addEventListener('click', () => randomizeData());
    }

    setupReactivity() {
      // Chart: chart_7
      const chartConfig_chart_7 = {"id": "chart_7", "chartType": "pie", "dataProvider": "browserShare", "horizontalAxis": null, "verticalAxis": null, "series": [{"type": "pie", "isArea": false, "xField": "", "yField": "", "field": "value", "nameField": "name", "displayName": "Market Share"}]};
      let chart_chart_7 = null;
      createEffect(() => {
        const container = this.shadowRoot.getElementById('chart_7');
        if (!container) return;
      
        const data = browserShare.value || [];
      
        if (!chart_chart_7) {
          chart_chart_7 = OpenFlexCharts.createChart(container, chartConfig_chart_7);
        }
      
        OpenFlexCharts.updateChartData(chart_chart_7, data, chartConfig_chart_7);
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_10');
        if (el) el.textContent = `<PieChart dataProvider='${browserShare.value}' width='500' height='350'>`;
      });
    }
  }

  // Register Custom Element
  if (typeof window !== 'undefined') {
    customElements.define('app-root', AppComponent);
  }

  // Export for Node.js/testing
  if (typeof module !== 'undefined' && module.exports) {
    module.exports = { AppComponent };
  }

})();