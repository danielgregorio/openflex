// Generated by OpenFlex Neo MXML Compiler
// Complete MXML -> Web Components with Full Reactivity

(function() {

  // Generated by OpenFlex Neo
  // AS4 -> JavaScript

  // OpenFlex Reactivity Runtime
  const { Signal, Computed, createEffect } = (typeof window !== 'undefined' && window.OpenFlexRuntime)
    ? window.OpenFlexRuntime
    : require('./runtime/openflex-runtime.js');

  const budgetData = new Signal([]);

  async function loadExternalData() {
    try {
      const response = await fetch('./doughnut-chart-demo-data.json');
      const data = await response.json();
      budgetData.value = data.budgetData;
    } catch (error) {
      console.error('Error loading data:', error);
    }
  }

  const totalBudget = new Computed(() => {
    const total = budgetData.value.reduce((sum, d) => sum + d.value, 0);
    return "R$ " + total.toLocaleString('pt-BR');
  });

  function randomizeData() {
    budgetData.value = budgetData.value.map(d => ({ ...d, value: Math.floor(Math.random() * 40000) + 15000 }));
  }


  // OpenFlex Charts Runtime (ECharts wrapper)
  const OpenFlexCharts = (typeof window !== 'undefined' && window.OpenFlexCharts)
    ? window.OpenFlexCharts
    : require('./runtime/openflex-charts.js');

  // Application Component
  class AppComponent extends HTMLElement {
    constructor() {
      super();
      this.attachShadow({ mode: 'open' });
    }

    connectedCallback() {
      this.render();
      loadExternalData().then(() => {
        this.setupReactivity();
      });
    }

    render() {
      this.shadowRoot.innerHTML = `
        <link rel='stylesheet' href='../runtime/neo-flex-classic-theme.css'>
        <link rel='stylesheet' href='./doughnut-chart-demo.css'>
      <div id='app_0' class='neo-application'>
        <div id='panel_1' class='neo-panel'>
          <div class='neo-panel-header'>DoughnutChart - Grafico de Rosca</div>
          <div class='neo-panel-body'>
            <div id='vbox_2' class='neo-vbox' style='width: 100%; gap: 15px'>
              <span id='label_3' class='neo-label'>O DoughnutChart e similar ao PieChart mas com um furo no centro, util para destacar o total ou adicionar informacao extra.</span>
              <div id='hbox_4' class='neo-hbox h-align-right' style='width: 100%'>
                <button id='btn_5' class='neo-button'>Atualizar Dados</button>
              </div>
              <div id='vbox_6' class='neo-vbox h-align-center' style='width: 100%'>
                <span id='label_7' class='neo-label total-label'></span>
                <div id='chart_8' class='neo-chart' style='width: 500px; height: 350px'>
                  <!-- Chart rendered by ECharts -->
                </div>
              </div>
              <div id='panel_9' class='neo-panel'>
                <div class='neo-panel-header'>Codigo Fonte</div>
                <div class='neo-panel-body'>
                  <div id='vbox_10' class='neo-vbox' style='gap: 5px'>
                    <span id='label_11' class='neo-label'></span>
                    <span id='label_12' class='neo-label'>    <series></span>
                    <span id='label_13' class='neo-label'>        <PieSeries field='value' nameField='name' displayName='Alocacao' /></span>
                    <span id='label_14' class='neo-label'>    </series></span>
                    <span id='label_15' class='neo-label'></DoughnutChart></span>
                  </div>
                </div>
              </div>
              <div id='panel_16' class='neo-panel'>
                <div class='neo-panel-header'>Dados</div>
                <div class='neo-panel-body'>
                  <div id='vbox_17' class='neo-vbox' style='gap: 5px'>
                    <span id='label_18' class='neo-label'>@reactive var budgetData: Array = [</span>
                    <span id='label_19' class='neo-label'></span>
                    <span id='label_20' class='neo-label'></span>
                    <span id='label_21' class='neo-label'></span>
                    <span id='label_22' class='neo-label'></span>
                    <span id='label_23' class='neo-label'></span>
                    <span id='label_24' class='neo-label'>];</span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
      `;

      // Attach event handlers
      this.attachEventHandlers();
    }

    attachEventHandlers() {
      const el_btn_5 = this.shadowRoot.getElementById('btn_5');
      if (el_btn_5) el_btn_5.addEventListener('click', () => randomizeData());
    }

    setupReactivity() {
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_7');
        if (el) el.textContent = `Orcamento Total: ${totalBudget.value}`;
      });
      // Chart: chart_8
      const chartConfig_chart_8 = {"id": "chart_8", "chartType": "doughnut", "dataProvider": "budgetData", "horizontalAxis": null, "verticalAxis": null, "series": [{"type": "pie", "isArea": false, "xField": "", "yField": "", "field": "value", "nameField": "name", "displayName": "Alocacao de Orcamento"}]};
      let chart_chart_8 = null;
      createEffect(() => {
        const container = this.shadowRoot.getElementById('chart_8');
        if (!container) return;
      
        const data = budgetData.value || [];
      
        if (!chart_chart_8) {
          chart_chart_8 = OpenFlexCharts.createChart(container, chartConfig_chart_8);
        }
      
        OpenFlexCharts.updateChartData(chart_chart_8, data, chartConfig_chart_8);
      });
      createEffect(() => {
        const el = this.shadowRoot.getElementById('label_11');
        if (el) el.textContent = `<DoughnutChart dataProvider='${budgetData.value}' width='500' height='350'>`;
      });
    }
  }

  // Register Custom Element
  if (typeof window !== 'undefined') {
    customElements.define('app-root', AppComponent);
  }

  // Export for Node.js/testing
  if (typeof module !== 'undefined' && module.exports) {
    module.exports = { AppComponent };
  }

})();