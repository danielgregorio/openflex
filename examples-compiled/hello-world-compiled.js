// Generated by OpenFlex Neo MXML Compiler
// Complete MXML -> Web Components with Full Reactivity

// Generated by OpenFlex Neo
// AS4 -> JavaScript

// OpenFlex Reactivity Runtime
const { Signal, Computed, createEffect } = (typeof window !== 'undefined' && window.OpenFlexRuntime)
  ? window.OpenFlexRuntime
  : require('./runtime/openflex-runtime.js');

const message = new Signal("Hello, OpenFlex Neo!");

const year = new Signal(2025);

function greet() {
  trace(message.value + " - " + year.value);
}


// Application Component
class AppComponent extends HTMLElement {
  constructor() {
    super();
    this.attachShadow({ mode: 'open' });
  }

  connectedCallback() {
    this.render();
    this.setupReactivity();
  }

  render() {
    this.shadowRoot.innerHTML = `
    <div id='app_0' class='neo-application'>
      <div id='vbox_1' class='neo-vbox h-align-center v-align-middle' style='width: 100%; height: 100%'>
        <span id='label_2' class='neo-label'></span>
        <span id='label_3' class='neo-label'>Welcome to the future of ActionScript</span>
        <span id='label_4' class='neo-label' style='color: #666'>Built with Neo 🆕</span>
        <button id='btn_5' class='neo-button'>Click Me</button>
      </div>
    </div>
    `;

    // Attach event handlers
    this.attachEventHandlers();
  }

  attachEventHandlers() {
    const el_btn_5 = this.shadowRoot.getElementById('btn_5');
    if (el_btn_5) el_btn_5.addEventListener('click', () => greet());
  }

  setupReactivity() {
    createEffect(() => {
      const el = this.shadowRoot.getElementById('label_2');
      if (el) el.textContent = message.value;
    });
  }
}

// Register Custom Element
if (typeof window !== 'undefined') {
  customElements.define('app-root', AppComponent);
}

// Export for Node.js/testing
if (typeof module !== 'undefined' && module.exports) {
  module.exports = { AppComponent };
}